// @ts-check
import { defineConfig } from 'astro/config';

// Minimalny Astro: statyczna strona portfolio.
// site = docelowa domena (przyda się przy generowaniu absolutnych URL, sitemap itd.)
export default defineConfig({
  site: 'https://areksobczyk.com',
});
